<%- include('../layouts/admin/headadmin') %> <%-
include('../layouts/admin/adminHeader2') %> <%-
include('../layouts/admin/adminSidbar2') %>

<div class="main-panel">
  <div class="content-wrapper">
    <div class="row">
      <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
          <div class="card-body">
            <div class="row">
                <div class="col-lg-9">
                    <h4 class="card-title">Coupon Management</h4>
                     <p class="card-description">Manage Coupon detiails</p>
                </div>
                <div class="col-lg-3">
                    <button type="button" class="btn btn-inverse-info btn-icon" 
                    data-toggle="modal"
                    data-target="#addCouponModal"
                    
                    ><span>+</span>Add New Coupon</button>
                </div>
            </div>
            
            <!-- Flash Messages -->
            <% if (messages.success) { %>
            <div class="alert alert-success"><%= messages.success %></div>
            <% } %> <% if (messages.error) { %>
            <div class="alert alert-danger"><%= messages.error %></div>
            <% } %>
           

            <div class="table-responsive">
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th>Coupon Code</th>
                    <th>Discount(%)</th>
                    <th>Minimum Amount</th>
                    <th>Start Date</th>
                    <th>Expiry Date</th>
                    <th>Actions</th>
                  </tr>
                </thead>

                <tbody>
                  <% couponData.forEach(element => { %>
                  <tr>
                    <td><%= element.coupon_code %></td>
                    <td><%= element.discount %>%</td>
                    <td>₹ <%= element.minimum_amount %></td>
                    <td>
                      <script>
                document.write(new Date('<%= element.start_date %>').toLocaleDateString('en-GB'));
            </script>
                    </td>
                    <td>
                     <script>
                document.write(new Date('<%= element.expiry_date %>').toLocaleDateString('en-GB'));
            </script>
            </td>
                    <td>
                      <div
                        class="d-flex aligin-items-center justify-contern-center"
                      >
                        <!-- <div class="mx-2"> -->
                          <!-- Trigger button (Your <a> tag) -->
                          <!-- <a
                            href="#"
                            class="btn btn-primary btn-rounded btn-icon btn-sm"
                            data-toggle="modal"
                            data-target="#editCategoryModal"
                            onclick="populateModal('<%= element._id %>', '<%= element.name %>')"
                          >
                            <i class="fa-solid fa-pen-to-square"></i>
                          </a> -->
                        <!-- </div> -->

                        <div class="ml-2">
                          <% if ( element.is_delete == false) { %>
                          <a
                            href="/admin/couponManagment/blockCoupon/<%= element._id %>"
                            type="button"
                             class="btn btn-secondary btn-rounded btn-icon btn-sm"
                          >
                            <i class="fa-solid fa-trash-can"></i>
                          </a>
                          <% }else { %>
                          <a
                            href="/admin/couponManagment/unBlockCoupon/<%= element._id %>"
                            type="button"
                           
                            class="btn btn-danger btn-rounded btn-icon btn-sm"
                          >
                            <i class="fa-solid fa-trash-can"></i>
                          </a>

                          <% } %>
                        </div>
                        
                      </div>
                    </td>
                  </tr>
                  <% }) %>
                </tbody>
              </table>

             
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Modal Structure -->
<div
  class="modal fade"
  id="addCouponModal"
  tabindex="-1"
  aria-labelledby="editCategoryModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="editCategoryModalLabel">Add New Coupon</h5>
        <button
          type="button"
          class="close-btn"
          data-dismiss="modal"
          aria-label="Close"
        >
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <form id="editCategoryForm" method="post" action="/admin/couponManagment" onsubmit="return validDetailsModal()">
          <div class="form-group">
            <label for="categoryName">Coupon Code</label>
            <input
              type="text"
              class="form-control"
              id="coupon_code"
              name="coupon_code"
              
            />
            <p id="coupon-error" style="color: brown; font-size: small"></p>
          </div>
          <div class="form-group">
            <label for="categoryName">Discount</label>
            <input
              type="text"
              class="form-control"
              id="discount"
              name="discount"
              
            />
            <p id="discount-error" style="color: brown; font-size: small"></p>
            <div class="form-group">
            <label for="categoryName">Minimum Amount</label>
            <input
              type="text"
              class="form-control"
              required
              name="minimum_amount"
              
            />
          </div>
          </div>
          <div class="form-group">
            <label for="categoryName">Start Date</label>
            <input
              type="date"
              class="form-control"
              required
              name="start_date"
              
            />
          </div>
          <div class="form-group">
            <label for="categoryName">Expiry Date</label>
            <input
              type="date"
              class="form-control"
              required
              name="expiry_date"
              
            />
          </div>
          <button type="submit" class="btn btn-primary">Submit</button>
        </form>
      </div>
    </div>
  </div>
</div>

<script>
  function formatDate(date) {
    return date.toLocaleDateString("en-GB");
  }


    function validDetailsModal() {
    var name = document.getElementById("coupon_code").value;
    let discount = document.getElementById('discount').value;  
    document.getElementById('coupon-error').textContent = '';
    document.getElementById('discount-error').textContent = '';

    if (!name || name.trim().length == 0) {
      document.getElementById("coupon-error").innerHTML = "Field is required";
      return false;
    }
    if (!discount || discount.trim().length == 0) {
      document.getElementById("discount-error").innerHTML = "Field is required";
      return false;
    }

    return true;
  }

  function populateModal(categoryId, categoryName) {
    // Set the form action dynamically based on the category ID
    document.getElementById("editCategoryForm").action =
      "/admin/catetoryManagment/editCoupon/"+categoryId;

    // Set the current category name in the input field
    document.getElementById("categoryName").value = categoryName;
  }
</script>

<%- include('../layouts/admin/footadmin') %>
